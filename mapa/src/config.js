// config.js

const TILE_SIZE = 16;

export const CONFIG ={
    TILE_SIZE: TILE_SIZE,
    GAME_WIDTH: 18 * TILE_SIZE,
    GAME_HEIGHT: 27 * TILE_SIZE,
    GAME_SCALE: 1.6,
    DEBUG_COLLISION: true // true = mostra a área dos colliders
}